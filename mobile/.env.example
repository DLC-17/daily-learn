# Copy to .env and fill in your values.
# For EAS cloud builds (preview/production), set EXPO_PUBLIC_API_URL as an EAS secret:
#   eas env:create --name EXPO_PUBLIC_API_URL --value https://your-api-url

EXPO_PUBLIC_API_URL=http://localhost:8080
